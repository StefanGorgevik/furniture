import { Tooltip } from "@material-ui/core";

const EuroIcon = () => {
  return (
    <Tooltip title="Price">
      <i className="fas fa-euro-sign"></i>
    </Tooltip>
  );
};

export default EuroIcon;
